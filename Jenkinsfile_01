pipeline {
    agent any

    triggers {
        pollSCM('H/5 * * * *') {
            excludedRegions 'path/to/ignore/**'
        }

    stages {
        stage('Build') {
            steps {
                sh 'echo 1'
            }
        }
    }
}
