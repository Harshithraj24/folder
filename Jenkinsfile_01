pipeline {
    agent any

    triggers {
        pollSCM('H/5 * * * *') {
            includedRegions 'test2'
        }
 }
    stages {
        stage('Build') {
            steps {
                sh 'echo 1'
            }
        }
    }
}
