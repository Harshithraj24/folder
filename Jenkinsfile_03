pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
            checkout([
                    $class: 'GitSCM', 
                    includedRegions: 'test3', 
                    workspaceUpdater: [$class: 'CheckoutUpdater']
                ])
                sh 'echo 3'
            }
        }
    }
}
